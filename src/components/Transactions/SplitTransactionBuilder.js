const SplitTransactionBuilder = (props) => {
  const { title, visible, onOk, onCancel, accounts, accountId, transaction, } = props;  
  return (
    <></>
  )
}

export default SplitTransactionBuilder;